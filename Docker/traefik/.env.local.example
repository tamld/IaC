# Copy to .env.local and fill with your real secrets; DO NOT commit .env.local
# Use this example to document required variables only.
CF_API_EMAIL=your-email@example.lab
CF_API_KEY=your-real-cloudflare-api-token
DOMAIN=yourdomain.example.lab
BASIC_AUTH_USER=admin
# BASIC_AUTH_PASSWORD_HASH should contain the bcrypt/htpasswd-style password hash only.
# Generate a bcrypt htpasswd value locally (example):
#   Install apache2-utils (Debian/Ubuntu) or httpd-tools (RHEL/CentOS), then run:
#     printf "%s" "admin:$(openssl passwd -apr1 yourpassword)" | cut -d":" -f2
#   Or using htpasswd (bcrypt) if available:
#     htpasswd -nbB admin yourpassword | cut -d":" -f2
# Paste only the hash value below; do NOT commit real secrets to the repository.
BASIC_AUTH_PASSWORD_HASH=
